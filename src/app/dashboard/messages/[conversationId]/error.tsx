'use client'

// Legacy route boundary — kept until Track 6 deletes the dashboard tree.
// Renders nothing because the parent page issues an immediate redirect.
export default function LegacyConversationError() {
  return null
}
